/**
 * 1. create a base url(BASE_URL=/api/v1) inside the .env file
 * 2. then create routes folder. and create a index file, index.js
 * 3. then base url add to index.js file inside the routes 
 * 4. then create api folder and create a api index file, index.js
 * 5. now call api folder inside the index.js file main routes index.js file. (const apiRoutes = require('./api'))
 * 6. now create authRoutes.js file and create api's.
 * 7. then connect api route main index.js file. (app.use(route))
 * 
 * */ 